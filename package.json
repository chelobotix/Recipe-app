{
  "devDependencies": {
    "stylelint": "~13.0.0",
    "stylelint-config-standard": "~21.0.0",
    "stylelint-csstree-validator": "~1.0.0",
    "stylelint-scss": "~3.0.0"
  },
  "scripts": {
    "lint:css": "stylelint \"**/*.{css,scss}\" --fix",
    "lint:fix": "pnpm run lint:css && rubocop -A",
    "tw:dev": "rails tailwindcss:watch"
  }
}
